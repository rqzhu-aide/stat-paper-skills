# Weighted Outcome Study

## Introduction

We target the population mean under a covariate-shift model. The draft later
calls this same quantity a prediction error, without defining a prediction
problem.

TODO: replace this sentence with the final contribution claim.

## Method

The method defines (mu=mathbb{E}_Q(Y)), but the algorithm description calls
(mu) the variance of the importance weights and interprets its estimate as a
dispersion measure. The remaining prose uses the population-mean definition.
