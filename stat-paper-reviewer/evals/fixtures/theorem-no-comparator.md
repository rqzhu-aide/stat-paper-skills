# Fast Mean Estimation

## Model and estimator

Let (X_1,ldots,X_n) be independent (N(	heta,1)) observations, and let
(widehat{	heta}_n=n^{-1}sum_{i=1}^n X_i).

## Main result

**Theorem 1.** For every integer (ngeq 1),
[
sup_{	hetainmathbb{R}}
mathbb{E}_{	heta}left[
  left(widehat{	heta}_n-	hetaight)^2
ight]
leq n^{-2}.
]

The proof sketch says that Gaussian concentration gives an (n^{-1}) deviation
and that averaging the observations supplies another factor of (n^{-1}).
No lower bound, comparison theorem, citation, or full proof is supplied.
