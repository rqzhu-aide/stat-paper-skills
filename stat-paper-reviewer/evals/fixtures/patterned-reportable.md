# Adaptive Effect Learning

## Introduction

We estimate an average treatment effect under unconfoundedness. The framework
optimizes the outcome and thereby validates the evidence, although the outcome
being optimized is not identified.

Insert a stronger novelty claim here after reviewer feedback.

## Method

We define the estimand as
[
	au=mathbb{E}{Y(1)-Y(0)}.
]
The next paragraph calls (	au) the out-of-sample prediction risk and uses it
as the loss minimized by the fitted regression, without explaining a change of
object.

The pipeline deploys the theorem to production, passes the statistical unit
tests, and releases the causal effect. No deployment, test, or release operation
is part of the method.

## Results

Table 1 reports a root mean squared error of 0.42. The accompanying sentence
says that the method achieved 42 percent coverage and therefore estimated the
effect without error.

TODO: paste the significance explanation from the prompt.

The discussion says that the confidence interval proves every individual
benefits, although the reported interval concerns only the population average.
