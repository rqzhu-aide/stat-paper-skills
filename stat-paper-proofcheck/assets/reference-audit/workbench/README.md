# Reference workbench provenance

The top-level annotations are the exact final-context annotations from the
surgical validator-maintenance renewal on 8 September 2026. The complete
source and mathematical evidence were re-reviewed in
`/root/surgical_reference_renewal`; source and mathematical judgments remain
unchanged. The annotations were renewed through fresh extraction and the public
packet, rebind and compile workflow using the same calibration receipt.
Their initial proofcheck 1.5 scaffolding and qualification remain historical
provenance from `/root/renew_release_reference`. A separate fresh
context, `/root/reference_qualification`, fixed all five canary responses
before balanced grading; all passed. The declared profile is
`gpt-6-astra-inherited`, configuration `proofcheck-1.5-inherited-settings`.
The current qualification receipt is linked in each annotation file and
the actual grading command receipt is `calibration-receipt-1.5.json`.
These declarations are provenance, not automatic identity attestation or
a research benchmark result.

The actual independent challenge responses and their exact consumed packets
are preserved inside the portable audit under audit/05_adversarial/initial-*.json.
The fixed records include earlier findings, genuine contract-3 responses with
both judgment dimensions, and superseded review history. The theorem's earlier
argument-status disagreement is explicitly resolved without editing its first response.
The primary review and reconciliation are explained in
proofcheck-audit/audit/07_runtime/PRIMARY_REFRESH_1_4.md and
proofcheck-audit/audit/07_runtime/PRIMARY_RENEWAL_2026_09_07.md.

Earlier annotation and canary-response bytes remain in history/protocol-1.3.
The annotations preceding the earlier renewal remain in history/pre-renewal-2026-09-07.
The source-and-reader repair annotations follow an actual new primary review;
their predecessors remain in history/pre-usability-repairs-2026-09-07.
The current independent responses agree on both invalid arguments and refuted
statements, while retaining the previous theorem disagreement in history.
Do not silently bind those historical judgments to a new source or calibration.

The final unreadable auxiliary-context regression repair required another
actual primary reconfirmation and recompilation under the current validator.
Those predecessor annotations are preserved in
`history/pre-context-recovery-2026-09-07`. The source and judgments are unchanged;
the new source-bound primary review is recorded in
`proofcheck-audit/audit/07_runtime/PRIMARY_CONTEXT_RECOVERY_2026_09_07.md`.
A separate fresh packet-only reviewer supplied the new current initial
responses. Prior disagreements and reconciliation evidence remain linked.

The immediate predecessors and prior top-level canary responses are preserved
in `history/pre-proofcheck-1.5-renewal-2026-09-08`. Both current independent responses
were produced by new packet-only contexts, preserved before reconciliation,
and linked to all earlier reviews. The primary source review is recorded in
`proofcheck-audit/audit/07_runtime/PRIMARY_RENEWAL_1_5_2026_09_08.md`.

The current maintenance predecessors, including all former top-level workbench
inputs and this README, remain byte-exact in `history/pre-surgical-maintenance-2026-09-08`.
No new calibration is claimed. Actual fresh final-context independent responses
were preserved before reconciliation, agreed with all primary conclusion dimensions,
and were bound through the public CLI. The current primary and context records are
`proofcheck-audit/audit/07_runtime/PRIMARY_SURGICAL_REVIEW_2026_09_08.md` and
`proofcheck-audit/audit/07_runtime/FINAL_SURGICAL_CONTEXT_2026_09_08.md`.
