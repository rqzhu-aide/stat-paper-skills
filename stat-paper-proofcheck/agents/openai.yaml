interface:
  display_name: "Statistical Paper Proofcheck"
  short_description: "Rigorous non-formal statistical proof audits"
  default_prompt: "Use $stat-paper-proofcheck to inventory this paper's proof system, check prerequisites before dependents line by line, close every dependency and assumption, independently verify every in-scope unit from a fresh context, and give a concise evidence-backed summary within the stated non-formal scope."
policy:
  allow_implicit_invocation: false
